# AGENTS.md

## 作業方針

- このプロジェクトの調査、要件整理、設計判断、実装、テスト、レビュー、統合は、現在の担当エージェントが直接行う。
- 新たな子ワーカー、通常タスク、子スレッド、サブエージェントを作成してはならない。`spawn_agent` を使用しない。作業の再委任もしない。
- 不明確な要件やユーザー判断が必要な変更は、実装前に選択肢と影響をユーザーへ示す。確定した方針の範囲では重複確認を避けて進める。

## プロジェクトの境界

- `manuscripts/` のMarkdownと `data/` のYAMLを唯一の正本とする。HTML、PDF、JSON、中間データは生成物または交換形式であり、正本として手編集しない。
- gift/ は旧原稿・Theme・PDFの参照元として扱う。特に指定がない限り変更せず、通常のbuild入力や旧HTML/CSS構造の互換実装へ戻さない。
- 意味的HTML契約とThemeの責務を分離する。構造・アクセシビリティの変更は契約とテストを併せて更新し、見た目の調整は可能な限りThemeで行う。
- A5での安定を優先する。A4対応は維持するが、A5の受け入れ品質が固まる前にA4最適化を優先しない。
- Markdown記法は、構造上必要な場合を除き、自然で読みやすいプレーンMarkdownに近づける。

## 変更と検証

- 作業前に `git status` と対象ファイルを確認し、既存のユーザー変更を保持する。無関係な変更を取り込んだり戻したりしない。
- 実装・文書・テストは依頼された範囲に限定し、変更リスクに見合う型検査、テスト、build、差分確認を行う。
- ユーザーの指示どおり、まとまりのある作業単位でGitコミットを作成する。コミット前に対象差分を確認し、依頼外の変更を含めない。
- 破壊的操作やスコープの曖昧な外部変更は行わず、必要な場合は先にユーザーへ確認する。
